# v1.1 Final Limitations (hardening pass)

Independent post-program engineering by Eric Raymond (2026). No University of
Wyoming, HUMANS MOVE, Purdue, or paper-author involvement.

## What this hardening pass verified (executed, with evidence)

- The Cartesian CBF-QP now solves an **exact, KKT-verified** QP, cross-validated
  against a trusted OSQP + SciPy-linprog oracle on 6000 seeded problems (0
  mismatches). See `qp-correctness-audit.md`, `results/v1.1-hardening/`.
- Truthful solver status; projection fallback never labeled optimal; default is
  to **stop** on exact-solver failure.
- An **independent discrete-step validator** is the final authority on every
  accepted command; a fresh external validator confirms safety over 500+
  randomized calls.
- Velocity limit is an honest **per-axis box**, not a Euclidean cap.
- Frames/units are explicit; the previously undocumented `z*0.5` is the named
  `image_relative_z_scale`.
- 143 pure-logic + property + oracle tests pass; CI runs the oracle smoke.

## What remains NOT done / NOT claimed (blocked or deferred)

| Area | Status | Blocker / note |
|---|---|---|
| End-to-end ROS 2 fake-hardware RViz recording w/ live diagnostics | not done | no ROS 2 / MoveIt / display in the audit VM |
| Verified simulated Panda IK (MoveIt `compute_ik`) + JointState evidence | not done | same; `sew_orientation` stays feature-only, `joint_positions=None` |
| ROS 1/2 parameter wiring of retargeter/safety/gate + launch tests | not done | needs a ROS host to run launch/integration tests |
| Paired ≥30-replicate benchmark, CIs, per-trajectory, fault injection | not done | see `paired-experiment-protocol.md`; committed `results/v1.1/` are stale vs the hardened solver |
| Versioned config schema validation at startup | not done | deferred |
| README / case-study numeric re-audit | not done | deferred until fresh paired benchmark exists |
| Physical Franka accuracy / collision-free execution / metric depth | out of scope | never claimed |

## Model-vs-reality boundary (standing)

The CBF filter models a **point** end-effector, **spherical** obstacles, and an
**axis-aligned** workspace box, in **discrete time** (continuous-time-inspired
barrier, enforced by the next-state validator). It is not a dynamics-level CBF,
not a forward-invariance certificate, and not a substitute for Franka Desk, a
physical E-stop, a dead-man control, and lab procedures. Defaults remain
`use_robot=false`, `dry_run=true`, physical output disabled, stop on solver
failure, hold-then-stop on stale/invalid input.
